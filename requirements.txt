# Glassbox — Multimodal Model Visualizer
# Extraction-first, animate-second. Versions pinned for reproducibility.

# Visualization
manim==0.18.1
manim-ml==0.2.5

# ML extraction (transformers >=4.40 for stable output_hidden_states API
# and CLIPModel.image_embeds/text_embeds)
transformers>=4.40.0,<5.0.0

# Torch — install the platform-specific wheel separately on macOS:
#   python -m pip install torch torchvision torchaudio
# We pin a minimum here; the lock comes from the system wheel.
torch>=2.1.0

# Numerics / ML
numpy>=1.24.0,<2.1.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Audio
librosa>=0.10.0
soundfile>=0.12.0

# Imaging
Pillow>=10.0.0

# Tests
pytest>=7.4.0
